Lead Architect: The project scope has officially evolved into a Generalized Quantum Physics Engine, as discussed in our strategic calibration. 

Execute the following orchestrated plan to move beyond the single-atom model:

1. Blueprint Expansion: Read @ARCH.md and @TODO.md. Update the 'PhysicsEngine' section in ARCH.md to reflect a modular framework capable of simulating:
   - Wave-Particle Duality (Double-slit interference, wave-packet propagation).
   - Spin & Superposition (Pauli matrices, Bloch sphere representation).
   - Entanglement (Multi-particle state vector mapping).
   - Laser Physics (Stimulated emission and population inversion).

2. Knowledge Update: Spawn the `physicist` agent. Instruct it to read and rewrite @skills/physics_engine_skill.md. It must include implementation rules for the Time-Dependent Schrödinger Equation (TDSE) for spatial interference, Pauli spin matrices for discrete states, and Dirac notation (Bra-Ket) logic for state vectors.

3. Modular Scaffolding: The physicist must now architect the following C++ files in `PhysicsEngine/`:
   - `IQuantumObject.hpp`: An interface for anything that has a wavefunction or state vector.
   - `WavePacket.cpp/h`: For duality and interference labs.
   - `SpinSystem.cpp/h`: For superposition and entanglement labs.
   - `HydrogenModel.cpp/h`: A specialized implementation of the general framework.

4. Mathematical Verification: The physicist must implement a "Math Proof" in `src/main.cpp` (console-only). It must calculate a 1D interference pattern of two gaussian wave-packets and print the resulting probability density at the center point to prove constructive/destructive interference is working.

5. Team Handover: Once the math is verified and the build succeeds, update @TODO.md with these new generalized tasks and wait for my instruction.